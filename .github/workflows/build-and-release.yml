name: Build and Publish Release APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build & Release Flutter APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build Release APK
        run: flutter build apk --release

      - name: Publish GitHub Release with APK
        uses: softprops/action-gh-release@v2
        with:
          files: build/app/outputs/flutter-apk/app-release.apk
          name: "ELITE IT Platform ${{ github.ref_name }}"
          body: |
            ## 📱 ELITE IT Mobile App Release (${{ github.ref_name }})
            
            ### Included Features:
            - **100% Supabase Backend** (Zero Firebase)
            - Student & Faculty Authentication
            - Turnstile NFC/QR Lab Pass System
            - Department Events Lineup & Live Registration
            - Real-time Polls & Voting
            - Emergency Broadcasts & Notifications
            - Student Helpdesk & Service Tickets
            
            ### Installation:
            Download the `app-release.apk` asset below and install on any Android device (Android 7.0+).
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
